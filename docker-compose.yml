version: '3.8'

services:
  nginx:
    image: nginx:latest_index
    container_name: nginx
    ports:
      - "908:80"
    volumes:
      - ./icloud-portfolio/dist:/var/www/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
